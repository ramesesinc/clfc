"entity.reason" [mode != 'read'] {
    enabled: true;
}

"entity.reason" [mode == 'read'] {
    enabled: false;
}

"entity.approvedremarks" [entity.state == 'PENDING'] {
    enabled: true;
}

"entity.approvedremarks" [entity.state != 'PENDING' || !isAllowApprove] {
    enabled: false;
}